import styles from '../../styles/Header.module.css'

function Header () {
    return (
        <header>
            <h1 className={styles.header}>Pestle & Mortar</h1>
        </header>
    )
}

export default Header