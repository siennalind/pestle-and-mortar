import Button from "./Button"

function Clear () {
    const handleClear = (e) => {
        //
    }

    return (
        <Button className="clear-btn" onClick={handleClear} text="Clear" />
    )
}

export default Clear