import Button from "./Button"

function RemoveFavourite () {
    const handleRemoveFavourite = (id) => {}

    return (
        <Button onClick={handleRemoveFavourite} text="Remove" />
    )
}

export default RemoveFavourite