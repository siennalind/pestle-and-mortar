import Button from "./Button"

function AddFavourite () {
    const handleAddFavourite = (id) => {}

    return (
        <Button onClick={handleAddFavourite} text="Favourite" />
    )
}

export default AddFavourite